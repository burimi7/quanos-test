name: Quasar Weather App CI/CD

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # Step 1: Get your code
      - name: Checkout code
        uses: actions/checkout@v3
      
      # Step 2: Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'
      
      # Step 3: Install dependencies
      - name: Install dependencies
        run: npm ci
      
      # Step 4: Run linting
      - name: Lint code
        run: npm run lint
      
      # Step 5: Build the app
      - name: Build Quasar app
        run: npx quasar build
        env:
          VITE_API_KEY: ${{ secrets.VITE_API_KEY }}
      
      # Step 6: Upload build as artifact
      - name: Upload build artifacts
        uses: actions/uname: Quasar Weather App CI/CD

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # Step 1: Get your code
      - name: Checkout code
        uses: actions/checkout@v4  # Also update to v4
        
      # Step 2: Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4  # Update to v4
        with:
          node-version: '20'
          cache: 'npm'
      
      # Step 3: Install dependencies
      - name: Install dependencies
        run: npm ci
      
      # Step 4: Run linting
      - name: Lint code
        run: npm run lint
      
      # Step 5: Build the app
      - name: Build Quasar app
        run: npx quasar build
        env:
          VITE_API_KEY: ${{ secrets.VITE_API_KEY }}
      
      # Step 6: Upload build as artifact - NOW USING V4
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4  # Changed from v3 to v4
        with:
          name: quanos-test-build
          path: dist/spa
          if-no-files-found: error  # Optional: fail if no files found
          retention-days: 5          # Optional: keep for 5 days
      name: Quasar Weather App CI/CD

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # Step 1: Get your code
      - name: Checkout code
        uses: actions/checkout@v4
        
      # Step 2: Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      # Step 3: Install dependencies
      - name: Install dependencies
        run: npm ci
      
      # Step 4: Run linting
      - name: Lint code
        run: npm run lint
      
      # Step 5: Build the app
      - name: Build Quasar app
        run: npx quasar build
        env:
          VITE_API_KEY: ${{ secrets.VITE_API_KEY }}
      
      # Step 6: Upload build as artifact
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: quanos-test-build
          path: dist/spa
          if-no-files-found: error
          retention-days: 5
      
      # Step 7: Build Docker image
      - name: Build Docker image
        run: docker build -t quanos-test .
      # Step 7: Build Docker image
      - name: Build Docker image
        run: docker build -t quanos-test .pload-artifact@v3
        with:
          name: quanos-test-build
          path: dist/spa
      
      # Step 7: Build Docker image
      - name: Build Docker image
        run: docker build -t quanos-test .